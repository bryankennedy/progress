# Template for local development secrets. Copy to .env and fill in real values.
# .env is gitignored; production secrets go in Cloudflare via `wrangler secret`.

# Shared secret for verifying GitHub webhook signatures (X-Hub-Signature-256)
GITHUB_WEBHOOK_SECRET=
