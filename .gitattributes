# The decision log's merge=union entry retired with PROG-91: decisions now live
# one-file-per-work-element under docs/decisions/, so parallel branches write
# different files and need no merge driver. Union would be a hazard on the
# remaining shared docs — it silently keep-boths conflicting edits.
